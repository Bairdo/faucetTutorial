cleanup () {
    for br in $(sudo ovs-vsctl list-br); do
        sudo ovs-vsctl del-br $br
    done
    IFS=$'\n';
    for i in  $(sudo ip netns list); do
        echo $i | cut -d ' ' -f1 | xargs sudo ip netns delete ;
    done
}
